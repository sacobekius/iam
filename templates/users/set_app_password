{% extends "base.html" %}

{% block content %}
Wijzig wachtwoord voor alle testgebruikers van {{ application }}:
<form method="post" id="password_form" action="{% url 'set-app-password' application %}">
    {% csrf_token %}
    <label>
        Password:
        <input type="password" form="password_form" name="password">
    </label>
    <label>
        Check:
        <input type="password" form="password_form" name="checkpassword">
    </label>
    <button class="gwknop" form="password_form" type="submit" name="event" value="Wijzig">Wijzig</button>
</form>
{{ message }}
{% endblock %}